# Upstream references (vendors removed)

Temporary local clones were deleted after Phase 0. Use these URLs if you need prior-art details for Phase 1+:

| Project | URL | What we used |
|---|---|---|
| Cabret-Log | https://github.com/Foso/Cabret-Log | IR enter/exit transform ideas (archived; rewrite for Kotlin 2.x) |
| Hugo | https://github.com/JakeWharton/hugo | `@DebugLog` UX, `⇢`/`⇠` log style |
| Hunter | https://github.com/Leaking/Hunter | Android debug enable patterns; ASM not our engine |

Our engine remains Kotlin IR (`compiler-plugin` + `gradle-plugin-build`).
